fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
# torch is only needed when embeddings.pt is present (Phase 4).
# Install separately: pip install torch --index-url https://download.pytorch.org/whl/cpu
