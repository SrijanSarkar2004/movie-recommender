# Backend environment variables
# Copy this file to .env.local and fill in any values needed.
# NEVER commit .env.local to version control.

# (No secrets are required by the backend in Phase 1-3.
#  Phase 4: if you add a TMDB server-side call, put the key here.)
# TMDB_API_KEY=
